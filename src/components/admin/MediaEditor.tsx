import { Box } from '@mui/material'

interface MediaEditorProps {
  type: 'image' | 'video'
}

const MediaEditor = (_props: MediaEditorProps) => {
  return (
    <Box>
      {/* 编辑功能将在这里实现 */}
    </Box>
  )
}

export default MediaEditor
